write a c program to find frequency of the given element arr=[10 20 10 20 30 20 40] input:10 output:2 times input:20 output:3 times using array concept in c language?
#include <stdio.h>
int main() {
    int arr[] = {10, 20, 10, 20, 30, 20, 40};
    int n = sizeof(arr) / sizeof(int);
    int x, count = 0;
    printf("Enter the element to search: ");
    scanf("%d", &x);
    for (int i = 0; i < n; i++) {
        if (arr[i] == x) {
            count++;
        }
    }
    printf("The frequency of %d is %d times.\n", x, count);
    return 0;
}

