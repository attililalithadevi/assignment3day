write a c program to merge two arrays and store it into third array a={1,2,3}, b={10,20,30}, c={1,2,3,10,20,30} using arrays concept in c language?
code:
#include <stdio.h>
int main() {
    int a[] = {1, 2, 3};
    int b[] = {10, 20, 30};
    int c[6];
    int i, j;
    for (i = 0; i < 3; i++) {
        c[i] = a[i];
    }
    for (j = 0; j < 3; j++) {
        c[i + j] = b[j];
    }
    printf("Merged array: ");
    for (i = 0; i < 6; i++) {
        printf("%d ", c[i]);
    }
    printf("\n");
    return 0;
}

